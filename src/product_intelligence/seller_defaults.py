SELLER_SKU_LABELS = {"seller sku", "sku vendedor", "sku del vendedor"}


def install() -> None:
    """Seller SKU stays protected during research; output default is applied at write time."""
    return None
